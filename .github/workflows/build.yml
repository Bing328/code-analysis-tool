name: Build Windows Executable

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🐍 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'
    
    - name: 📦 安装PyInstaller
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
    
    - name: 🔨 构建Windows EXE
      run: |
        echo "开始构建Windows可执行文件..."
        echo "工作目录: ${{ github.workspace }}"
        
        # 使用PyInstaller构建单一可执行文件
        pyinstaller --onefile --console `
          --name "CodeAuditor" `
          --clean `
          --distpath ./dist `
          --workpath ./build `
          --specpath ./build `
          --hidden-import urllib.request `
          --hidden-import urllib.error `
          --hidden-import urllib.parse `
          --hidden-import html `
          --hidden-import typing `
          professional_code_auditor_v2.py
        
        echo "构建完成！"
    
    - name: 🔍 验证构建结果
      run: |
        echo "验证生成的文件..."
        if (Test-Path ./dist/CodeAuditor.exe) {
          echo "✅ EXE文件已生成"
          $fileInfo = Get-Item ./dist/CodeAuditor.exe
          echo "文件大小: $([math]::Round($fileInfo.Length / 1MB, 2)) MB"
          echo "文件类型:"
          file ./dist/CodeAuditor.exe
        } else {
          echo "❌ EXE文件未生成"
          exit 1
        }
    
    - name: 📤 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: CodeAuditor-Windows
        path: |
          ./dist/CodeAuditor.exe
        retention-days: 30
